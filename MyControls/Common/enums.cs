using System;
using System.Collections.Generic;
using System.Text;

namespace MyControls.Common
{
    public enum WidthType
    {
        S = 16, M = 24, L = 32
    }
    public enum HeightType
    {
        S = 16, M = 24, L = 32
    }
    public enum SizeType
    {
        S = 16, M = 24, L = 32
    }
}
